// Compatibility entry for consumers that still import the historical PDF
// Editor path. The implementation now belongs to the lazy feature boundary.
export { initPdfEditorTool } from './features/pdf-editor/controller.js';
