// Compatibility entry for consumers that still import the V2 path.
// Runtime loading uses the feature-owned controller directly.
export { initTeleprompterTool } from './features/teleprompter/tool.js';
